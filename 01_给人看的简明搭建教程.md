# 给人看的简明搭建教程

## 1. 需要准备什么

| 类别 | 需要准备 | 关键说明 |
|---|---|---|
| 电脑 | Windows 10/11 64 位电脑；可用的 USB 口和网口 | 尽量直接连接电脑端口；首次排查 DP831 时不要先经过 USB 集线器 |
| 示波器 | RIGOL MSO5354 | 用普通以太网线接电脑或同一交换机/路由器 |
| SMU | Keithley 2400 | 使用**直通型 DB-9 RS-232 线**，不能使用 Null Modem 交叉线 |
| 串口适配器 | CH340/CH341 USB 转 RS-232 适配器 | 安装对应 Windows 驱动；COM 号由系统分配，不能写死 |
| 电源 | RIGOL DP831 | 使用 USB-A 转 USB-B **数据线**；USB-B 插后面板 **USB DEVICE**，不要插 USB HOST |
| 软件 | Python、NI-VISA、CH340 驱动、Python 库 | 下载入口见第 3 节 |

开始前先让三台仪器的输出保持关闭。这里的“连通”只指通信成功，不代表可以给 DUT 上电。

## 2. 整体连接方式

```text
MSO5354 ──以太网──> Windows ──PyVISA-py / pymso5000──> Codex MCP（可选）

DP831 ──USB-A↔USB-B──> Windows ──NI-VISA / USBTMC──> Python
               后面板 USB DEVICE

Keithley 2400 ──直通 DB-9 RS-232──> CH340 转换器 ──USB──> Windows COM 口
                                             └──PyVISA-py / pyserial
```

## 3. 推荐安装顺序

### 第一步：安装 Python

从 [Python Windows 官方下载页](https://www.python.org/downloads/windows/)下载安装 64 位 Python。为了复现本次已验证环境，建议使用 Python 3.14 系列；本机盘点版本为 3.14.4。

安装时勾选把 Python 加入 PATH。随后为项目建立独立虚拟环境，不要把所有库混装到系统 Python。

### 第二步：安装 NI-VISA

从 [NI-VISA 官方下载页](https://www.ni.com/en/support/downloads/drivers/download.ni-visa.html)选择与当前 Windows 兼容的版本。它主要负责 DP831 的 USBTMC 驱动和 VISA 通信。

如果安装器提供 NI MAX 或 VISA Interactive Control，建议一并安装，便于查看资源。安装后重启 Windows，再连接 DP831。

### 第三步：安装 CH340/CH341 驱动

从 [WCH 官方驱动页](https://www.wch-ic.com/downloads/CH341SER_ZIP.html)获取 Windows 驱动。安装后连接 USB 转 RS-232 适配器，在设备管理器中确认它出现为一个 COM 口。

### 第四步：安装 Python 库

在项目虚拟环境中安装 [requirements-validated.txt](./requirements-validated.txt) 中的版本。核心通信库是 PyVISA、PyVISA-py 和 pyserial；PyMeasure 是现有自动测试工程使用的上层库；pymso5000 负责 MSO5354 和可选的 MCP 服务。

### 第五步：需要 AI 直接访问示波器时，再配置 MCP

安装 `pymso5000[mcp]` 后，把示波器的**实际 IP**写进 Codex MCP 配置。详细做法和模板见 [02_给AI的详细安装与配置手册.md](./02_给AI的详细安装与配置手册.md)。

## 4. 三台仪器怎样接

### MSO5354 示波器

1. 用以太网线将示波器与电脑直连，或接入同一局域网。
2. 在示波器网络设置中查看实际 IP。
3. 在电脑上确认能到达该 IP。
4. VISA 资源通常写成 `TCPIP::<实际IP>::INSTR`。

历史环境中地址曾在重启或网络变化后改变，因此这里不保留具体地址。部署时必须从示波器界面重新读取，并使用 `ACTUAL_SCOPE_IP` 占位符替换。

常见固件中可以从 `Utility → I/O → LAN` 查看网络地址；不同固件的菜单名称可能略有不同。电脑端可以先运行：

```powershell
ping ACTUAL_SCOPE_IP
```

`ping` 成功只表示基本 IP 可达，不代表 VISA 或 MCP 已经可用；`ping` 失败也可能是 ICMP 被禁用。最终仍要按“资源发现 → 经授权的身份查询”继续确认。

#### MSO5354 专项快速路径

1. 安装 Python 3.14 x64，并建立独立虚拟环境。
2. 安装 `pymso5000[mcp]` 及其 PyVISA/PyVISA-py 依赖。
3. 用网线连接示波器和电脑，读取示波器当前 IP。
4. 检查基本网络和 `TCPIP::ACTUAL_SCOPE_IP::INSTR` 资源。
5. 将实际资源注册到 Codex STDIO MCP，并确认 MCP 工具能够加载。
6. 用户允许通信后再做身份查询；读取波形和修改参数分别授权。

### Keithley 2400 SMU

1. 使用直通型 DB-9 RS-232 线连接 2400 与 CH340 转换器。
2. 转换器插入电脑 USB 口。
3. 在 2400 前面板选择 RS-232 接口。
4. 设置为 9600 baud、8 data bits、no parity、1 stop bit、XON/XOFF、LF。
5. 在设备管理器查看当前 COM 号。

换 USB 口、重新安装驱动或重新枚举后 COM 号可能变化，所以程序应动态发现 CH340，不要固定写某个历史 COM 号。

### DP831 电源

1. USB-B 方口插 DP831 **后面板 USB DEVICE**。
2. USB-A 插电脑，优先直连电脑 USB 口。
3. 在设备管理器中确认它属于 USB Test and Measurement Device / USBTMC，而不是带黄色感叹号的未知设备。
4. 在 NI MAX、VISA Interactive Control 或 Python 中查找 USB VISA 资源。

USB VISA 资源通常类似 `USB0::<vendor-id>::<product-id>::<device-serial>::INSTR`。其中设备序列号属于具体仪器，必须从当前电脑重新发现。

## 5. 怎样判断“搭建完成”

按以下顺序逐级确认，不要跳级：

1. Windows 设备管理器能正确识别网卡/串口/USBTMC 设备。
2. VISA 或串口工具能列出对应资源。
3. 用户明确同意通信后，用最小的 `*IDN?` 查询确认型号。
4. 需要时再进行只读状态查询。
5. 修改量程、源值、触发或 OUTPUT 的操作必须单独授权。

在本地历史中曾确认的型号是 RIGOL MSO5354、Keithley Model 2400 和 RIGOL DP831。新环境必须读取自己的设备身份，不能只看资源名就认定成功。

## 6. 最常见的问题

| 现象 | 优先检查 |
|---|---|
| DP831 在设备管理器中 Code 28 | 是否接后面板 USB DEVICE；NI-VISA/IVI USBTMC 是否安装完成；重启后重新枚举 |
| DP831 的 LAN 选项是灰色 | 与 USB DEVICE 通信不是一回事；先按 USBTMC 路径排查 |
| DP831 偶发超时或 I/O 错误 | 换电脑直连 USB 口、换可靠数据线、避免多个程序同时打开同一资源 |
| Keithley 无响应 | 确认直通线而非 Null Modem；接口选 RS-232；9600/8N1/XON-XOFF/LF；确认当前 COM 号 |
| 示波器重启后 MCP 失效 | 重新查看示波器实际 IP，并更新 VISA 资源和 MCP 配置 |
| 能列出资源但不能查询身份 | 资源出现只证明系统发现了接口；还要检查驱动后端、串口参数、占用和超时 |

### AI 接入后能做什么

在权限允许的情况下，AI 可以按“选择通道 → 读取波形 → 计算 Vpp/频率/上升下降时间 → 判断异常 → 形成记录”的流程工作。修改通道、时基、触发或重新采集属于状态写入；必须另行授权，默认不能自动执行 Autoscale、Reset、AWG、按键或旋钮操作。

不要为了“让设备出现”就随意用 Zadig 把 DP831 改成 WinUSB/libusb 驱动。本次可用路径是 NI-VISA 提供的 IVI USBTMC 驱动；更换 Windows USB 驱动应先说明原因并获得确认。

## 7. 到这里就应暂停

完成设备识别和最小身份确认后，环境搭建已经结束。接线到 DUT、启用输出、设置电压/电流、自动扫描和记录测试数据属于下一阶段，必须依据电气连接、限值和安全规则另行执行。
