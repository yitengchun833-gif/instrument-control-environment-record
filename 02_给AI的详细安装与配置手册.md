# 给 AI 的详细安装与配置手册

## 前置步骤：获取仓库并确认工作目录

新电脑若尚未取得本仓库，先让用户确认允许下载并创建本地工作目录。此步骤只下载文档和依赖清单，不会连接、查询或控制仪器。

优先使用 Git 获取公开仓库；如电脑未安装 Git，可从 [Git for Windows 官方页](https://git-scm.com/download/win) 获取，或从 [GitHub 源码 ZIP](https://github.com/yitengchun833-gif/instrument-control-environment-record/archive/refs/heads/main.zip) 手动下载并解压。不要从第三方镜像站获取仓库或安装器。

在用户批准且目标目录不存在时，可执行：

```powershell
$repoRoot = 'D:\InstrumentEnvs\instrument-control-environment-record'
git clone --depth 1 https://github.com/yitengchun833-gif/instrument-control-environment-record.git $repoRoot
Set-Location -LiteralPath $repoRoot
Get-Item -LiteralPath .\README.md, .\requirements-validated.txt, .\02_给AI的详细安装与配置手册.md
```

若目录已存在，不要直接覆盖、删除或 `git pull`。先读取当前路径、远程地址和工作区状态，向用户说明差异并由用户决定保留、更新或另选目录。后续所有相对路径命令都必须在仓库根目录执行；也可以改用本节记录的 `$repoRoot` 绝对路径。

## 0. 任务目标与强制边界

目标是在 Windows 电脑上建立三条彼此独立、可审计的仪器通信链路：

- RIGOL MSO5354：Ethernet → TCP/IP VISA → PyVISA-py / pymso5000 → 可选 Codex MCP。
- Keithley 2400：RS-232 → CH340 USB 转串口 → COM → PyVISA-py / pyserial。
- RIGOL DP831：后面板 USB DEVICE → USBTMC → NI-VISA → PyVISA。

部署 AI 必须遵守以下边界：

1. 先下载和安装环境，再分别验证每台仪器；不要一上来运行芯片自动测试。
2. 未经用户明确授权，不连接或控制仪器，不发送 SCPI，不切换 OUTPUT，不修改电压、电流、量程、触发、耦合或采集状态。
3. 驱动安装和 MCP 配置属于会改变电脑状态的操作，执行前必须向用户列出来源、目标和影响，并取得明确同意。
4. 只从本手册列出的官方入口或其官方跳转页面下载；不要使用第三方驱动站、网盘或重新打包的安装器。
5. 不把历史 IP、COM 号、设备序列号写死。每台电脑都要重新发现。
6. 不用 Zadig 擅自把 DP831 的驱动替换为 WinUSB/libusb。先走 NI-VISA/IVI USBTMC 路径。
7. 保留安装器版本、文件名、下载 URL、安装时间、Windows 设备状态、VISA 资源名和错误原文。

## 1. 官方资料入口

### 驱动和应用

| 项目 | 官方入口 | 用途 |
|---|---|---|
| Git for Windows | https://git-scm.com/download/win | 获取仓库的可选命令行工具 |
| Python for Windows | https://www.python.org/downloads/windows/ | Python 运行环境 |
| NI-VISA | https://www.ni.com/en/support/downloads/drivers/download.ni-visa.html | DP831 USBTMC 驱动及 NI VISA 后端 |
| NI-VISA 用户手册 | https://www.ni.com/docs/en-US/bundle/ni-visa/page/user-manual-welcome.html | VISA 安装、资源和故障排查 |
| NI-VISA 兼容性 | https://www.ni.com/en/support/documentation/compatibility/17/ni-visa-features-and-operating-system-compatibility.html | 部署前核对 Windows 兼容性 |
| RIGOL 下载中心 | https://www.rigolna.com/support/downloads/ | 仪器手册和资料入口 |
| RIGOL 软件/UltraSigma | https://www.rigolna.com/support/software/ | 可选的 RIGOL 资源发现和 SCPI 工具 |
| WCH CH340/CH341 Windows 驱动 | https://www.wch-ic.com/downloads/CH341SER_ZIP.html | USB 转 RS-232 驱动 |

NI 下载页的最新版本、登录要求和操作系统列表会变化。AI 必须在部署当天读取页面并核对兼容性，不能把本记录中的旧离线安装包名称当成官方下载结论。

### 仪器手册

| 仪器 | 官方资料 | 本任务关注点 |
|---|---|---|
| RIGOL DP800 系列 | https://beyondmeasure.rigoltech.com/acton/attachment/1579/f-03a1/1/-/-/-/-/DP800%20Programming%20Guide.pdf | 后面板 USB DEVICE、USBTMC、UltraSigma、VISA 资源 |
| Keithley 2400 系列 | https://www.tek.com/en/keithley-source-measure-units/keithley-smu-2400-series-sourcemeter-manual/series-2400-sourcemeter | 手册入口 |
| Keithley 2400 用户手册 PDF | https://download.tek.com/manual/2400S-900-01_K-Sep2011_User.pdf | 直通 DB-9；RS-232 的 8N1、XON/XOFF、终止符 |

### Python 库和 MCP

| 项目 | 官方入口 | 本机验证版本 |
|---|---|---|
| PyVISA | https://pypi.org/project/PyVISA/ | 1.16.2 |
| PyVISA-py | https://pypi.org/project/PyVISA-py/ | 0.8.1 |
| pyserial | https://pypi.org/project/pyserial/ | 3.5 |
| PyMeasure | https://pypi.org/project/PyMeasure/ | 0.16.0 |
| pymso5000 | https://pypi.org/project/pymso5000/ | 0.2.0 |
| pymso5000 源码 | https://github.com/mabl/pyMSO5000 | MCP 和仪器库源码 |
| PyVISA 文档 | https://pyvisa.readthedocs.io/en/stable/ | 后端、资源和 API |
| Codex MCP 文档 | https://developers.openai.com/codex/mcp | STDIO MCP 注册和管理 |
| Codex 配置参考 | https://developers.openai.com/codex/config-reference | `config.toml` 字段 |

`pymso5000==0.2.0` 的包元数据要求 Python 3.14 或更高。本机已验证 Python 3.14.4 x64。如果将来选择其他版本，AI 要重新读取该版本的 `Requires-Python` 和依赖项。

## 2. 硬件和线材清单

### 必需

- 一台 Windows 10/11 64 位电脑，至少两个可用 USB 口和一个网口；也可通过可靠的局域网交换机连接示波器。
- RIGOL MSO5354 示波器和一根正常的以太网线。
- Keithley 2400 SMU。
- 一根与电脑端适配器和 Keithley 接口相匹配的**直通型 DB-9 RS-232 电缆**。手册明确要求 straight-through，不能用 Null Modem 交叉线；插头性别以实际接口为准。
- 一个 Windows 可识别的 CH340/CH341 USB 转 RS-232 适配器。
- RIGOL DP831 直流电源。
- 一根带数据芯线的 USB-A ↔ USB-B 线。USB-B 端接 DP831 后面板 **USB DEVICE**，不是 USB HOST。

### 推荐

- 备用的短 USB 数据线和备用电脑 USB 口，用于排除 DP831 偶发 I/O/超时。
- 独立标签，标记 DP831 USB DEVICE 线、Keithley 直通串口线和示波器网线。
- 能查看设备管理器、NI MAX/VISA Interactive Control 和 PowerShell 的本地管理员账户。
- 可选安装 VS Code 或其他编辑器；它只用于编写和查看代码，不是仪器连通的必要组件。

USB 集线器并非绝对禁止，但首次部署和故障排查时，应让 DP831 直连电脑。不要把 USB HOST/DEVICE、串口线或以太网线混为同一种“USB 连接”。

## 3. 交付架构与后端选择

```text
                         ┌─ PyVISA-py ─ pymso5000 ─ STDIO MCP ─ Codex
MSO5354 ─ Ethernet ─────┤
                         └─ TCPIP::<actual-ip>::INSTR

DP831 ─ USB DEVICE ─ USBTMC ─ NI-VISA ─ PyVISA(default NI backend)

Keithley 2400 ─ RS-232 ─ CH340 ─ Windows COM ─ PyVISA-py(@py) / pyserial
```

不要强制三台仪器使用同一个 VISA 后端：

- DP831 采用 NI-VISA，原因是本机最终由 IVI Foundation 的 USBTMC 驱动正确枚举。
- Keithley 串口链路采用 `ResourceManager("@py")`，减少对 NI 串口后端的依赖。
- MSO5354 的 TCP/IP 链路由 PyVISA-py 和 pymso5000 使用。

## 4. 安装前盘点（只读）

AI 在执行任何安装之前，先生成一张基线表：

| 项目 | 要记录的内容 |
|---|---|
| Windows | 版本、版本号、64 位、待重启状态 |
| Python | `python --version`、可执行文件路径、架构 |
| VISA | 已安装的 NI-VISA 版本；`visa32.dll` 是否存在；不要因此自动卸载旧版 |
| USB | DP831 的 VID/PID、设备状态、驱动提供商、INF、服务名 |
| 串口 | CH340 设备名称、硬件 ID、当前 COM 号、驱动版本 |
| 网络 | 示波器显示的实际 IP、电脑网卡 IPv4、是否同网段 |
| Codex | `codex mcp list` 的现有项目；备份现有相关配置片段，不覆盖其他 MCP |

如果发现已有 VISA/USB 驱动，AI 应先解释冲突风险，不得为了统一环境直接卸载或替换。

## 5. 安装步骤

### 5.1 Python 3.14 x64 和虚拟环境

1. 从 Python 官方 Windows 页面选择 64 位安装器。
2. 检查下载文件的发布者/签名和官方来源。
3. 安装 Python；建议启用 PATH，但仍以完整路径记录实际解释器。
4. 为仪器环境创建单独的虚拟环境，例如 `D:\InstrumentEnvs\instrument-suite`。

经用户批准后，可在 PowerShell 中执行：

```powershell
$repoRoot = 'D:\InstrumentEnvs\instrument-control-environment-record'
Set-Location -LiteralPath $repoRoot
py -3.14 -m venv D:\InstrumentEnvs\instrument-suite
D:\InstrumentEnvs\instrument-suite\Scripts\python.exe -m pip install --upgrade pip
D:\InstrumentEnvs\instrument-suite\Scripts\python.exe -m pip install -r "$repoRoot\requirements-validated.txt"
```

安装后只做离线版本盘点：

```powershell
D:\InstrumentEnvs\instrument-suite\Scripts\python.exe -m pip show pyvisa pyvisa-py pyserial pymeasure pymso5000
```

若电脑不需要复现当前版本，可选更新版本，但必须把“本地已验证基线”和“新选版本”分开记录，并重新验证依赖兼容性。

### 5.2 NI-VISA：为 DP831 提供 USBTMC

1. 在 NI 官方页面选择支持当前 Windows 的 NI-VISA。
2. 如果需要登录，暂停并让用户自行完成；不要索取或记录账号密码。
3. 安装 NI-VISA 运行时和所需开发支持；如安装器提供 NI MAX/VISA Interactive Control，建议安装。
4. 按安装器要求重启电脑。
5. 重启后再将 USB-B 接入 DP831 后面板 USB DEVICE，USB-A 直连电脑。
6. 在设备管理器中确认设备状态正常，类别类似 `USB Test and Measurement Device`，而不是 Code 28。

本机最终验证状态仅作对照：

- 硬件 ID：`USB\<vendor-id>&<product-id>\<device-serial>`
- 类别：`USBTestAndMeasurementDevice`
- 服务：`Usbtmc`
- 驱动提供商：`IVI Foundation, Inc`
- 原始 INF：`ausbtmc.inf`
- NI-VISA 后端：`C:\Windows\System32\visa32.dll`

设备序列号、INF 编号和驱动版本会随设备/系统不同，不能作为必须完全相同的验收条件。

### 5.3 CH340/CH341：为 Keithley 2400 提供串口

1. 从 WCH 官方页面下载 Windows 驱动。
2. 安装后插入 CH340/CH341 适配器。
3. 在设备管理器记录硬件 ID、驱动版本和 COM 号。
4. 将 Keithley 设置为 RS-232：9600 baud、8 data bits、no parity、1 stop bit、XON/XOFF、LF。
5. 使用直通型 DB-9 线，不得用 Null Modem。

某些 2400 设置接口时可能触发上电复位语义；因此必须在自动化开始前完成接口选择，并保持输出关闭。串口号可能随 USB 端口和系统枚举变化，部署逻辑必须以实时枚举为准。

### 5.4 MSO5354 网络

1. 用网线直连电脑或让示波器和电脑进入同一可信局域网。
2. 从示波器本机界面读取实际 IPv4 地址；常见固件入口为 `Utility → I/O → LAN`，菜单名称以实际固件为准。
3. 确认电脑网卡地址和路由合理；必要时为直连链路设置同网段静态地址。可以使用 `ping ACTUAL_SCOPE_IP` 做基本可达性参考，但 ping 不能替代 VISA 和身份查询验收。
4. 记录连接方式（直连/交换机/DHCP/静态）、电脑 IP、示波器 IP 和时间。
5. 构造 VISA 资源：`TCPIP::ACTUAL_SCOPE_IP::INSTR`。

历史工作中观察到链路本地地址会随重启、换网卡或重新协商而变化，因此公开教程不保留具体地址；每次部署都应重新发现实际地址。

## 6. 分级验收

每个阶段必须单独记录 Pass/Fail，不得因为“资源能列出”就写成“仪器已完全连通”。

| 等级 | 验收内容 | 是否与仪器通信 | 当前手册默认权限 |
|---|---|---:|---|
| L0 | 安装文件、Python 包、驱动版本和配置语法检查 | 否 | 可做 |
| L1 | Windows 设备枚举、COM/USBTMC/网卡状态 | 可能有系统枚举 | 可做只读盘点 |
| L2 | VISA/串口资源被发现 | 是/可能 | 需用户允许连接 |
| L3 | 发送 `*IDN?` 并核对制造商、型号 | 是 | 需明确授权 |
| L4 | 查询只读状态 | 是 | 需限定查询白名单 |
| L5 | 改变配置、源值、触发或 OUTPUT | 是且改变状态 | 必须逐项另行授权 |

注意：`SYST:ERR?` 会取出并消耗错误队列的一项，不是严格意义上的无状态读取。若使用它，必须明确说明副作用并记录读取前提和完整返回。

### 6.1 离线验证（不会打开仪器）

```powershell
D:\InstrumentEnvs\instrument-suite\Scripts\python.exe -c "import pyvisa, serial, pymeasure, pymso5000; print(pyvisa.__version__); print(serial.__version__)"
codex mcp list
```

`codex mcp list` 只检查当前 Codex 配置，不证明服务器能连接示波器。

### 6.2 资源枚举模板（会访问 VISA 后端）

只有用户允许开始通信后才运行：

```python
import pyvisa

print("NI-VISA resources:")
with pyvisa.ResourceManager() as rm_ni:
    print(rm_ni.list_resources())

print("PyVISA-py resources:")
with pyvisa.ResourceManager("@py") as rm_py:
    print(rm_py.list_resources())
```

该步骤不应并行打开同一台设备，也不应在枚举后自动发送初始化、复位或输出命令。

### 6.3 最小身份查询模板（会向仪器发送 SCPI）

只把目标资源替换成当场发现的值，并在用户授权后逐台执行：

```python
import pyvisa

resource = "REPLACE_WITH_DISCOVERED_RESOURCE"
backend = "@py"  # DP831 使用 NI-VISA 时删除此参数

with pyvisa.ResourceManager(backend) as rm:
    with rm.open_resource(resource) as inst:
        inst.timeout = 3000
        print(inst.query("*IDN?").strip())
```

Keithley 串口还要显式核对 9600/8N1/XON-XOFF/LF。身份查询成功只代表通信链路成立；不代表 DUT 接线、量程、保护限值或自动测试正确。

## 7. 安装与注册 MSO5354 MCP

### 7.1 安装

`requirements-validated.txt` 已包含 `pymso5000[mcp]==0.2.0`。单独安装时，在获批的虚拟环境中执行：

```powershell
D:\InstrumentEnvs\instrument-suite\Scripts\python.exe -m pip install "pymso5000[mcp]==0.2.0"
```

先确认服务器入口存在：

```powershell
Get-Item -LiteralPath D:\InstrumentEnvs\instrument-suite\Scripts\pymso5000-mcp.exe
```

### 7.2 通过 Codex 命令注册

将 `ACTUAL_SCOPE_IP` 替换为示波器当时显示的 IP：

```powershell
codex mcp add mso5000 -- D:\InstrumentEnvs\instrument-suite\Scripts\pymso5000-mcp.exe --resource "TCPIP::ACTUAL_SCOPE_IP::INSTR"
codex mcp list
```

如果已存在同名 MCP，先查看现有配置并让用户决定更新还是保留，不要覆盖。

### 7.3 直接配置 TOML

Codex 用户级配置通常在 `%USERPROFILE%\.codex\config.toml`。也可以按 Codex 官方文档使用项目级 `.codex/config.toml`。只追加或更新目标段，不改动其他 MCP：

```toml
[mcp_servers.mso5000]
command = 'D:\InstrumentEnvs\instrument-suite\Scripts\pymso5000-mcp.exe'
args = ["--resource", "TCPIP::ACTUAL_SCOPE_IP::INSTR"]
```

下面是另一套虚拟环境的通用示例：

```toml
[mcp_servers.mso5000]
command = 'D:\InstrumentEnvs\scope-only\Scripts\pymso5000-mcp.exe'
args = ["--resource", "TCPIP::ACTUAL_SCOPE_IP::INSTR"]
```

路径和地址均为模板，使用前必须替换为当前电脑的实际虚拟环境和示波器地址。

### 7.4 MCP 验收

MCP 验收也分级：

1. `codex mcp list` 能看到配置。
2. 新 Codex 会话能加载服务器和工具描述。
3. 用户允许仪器通信后，服务器能连接实际 VISA 资源。
4. 用户明确允许只读查询后，才调用限定的读取工具。
5. 不把“工具已出现”误写成“已读取波形”或“已完成自动测试”。

### 7.5 MSO5354 专项快速接入与能力边界

专项流程按以下顺序执行：

1. Python 3.14 x64 → 独立虚拟环境 → `pymso5000[mcp]`。
2. 网线连接 → 从示波器菜单读取实际 IP → 检查基本网络。
3. 使用 PyVISA-py 构造 `TCPIP::ACTUAL_SCOPE_IP::INSTR`，不要依赖系统默认后端。
4. 注册 Codex STDIO MCP → `codex mcp list` → 在新会话确认工具加载。
5. 用户明确允许通信后，才进入资源发现和 `*IDN?` 身份查询。

授权后的典型分析链可以是：选择通道 → 捕获波形 → 在本地保留数据上计算 Vpp、频率、上升/下降时间和异常指标 → 生成报告。读取真实仪器、重新采集、修改通道、时基或触发是不同权限层级。默认禁用 Autoscale、Reset、AWG、UI 按键/旋钮以及所有未列入白名单的写操作。

## 8. 故障分流表

| 编号 | 症状 | 诊断顺序 | 已知经验 / 限制 |
|---|---|---|---|
| T01 | DP831 出现 Code 28 | 确认后面板 USB DEVICE → 线材 → NI-VISA 安装/重启 → USBTMC 驱动状态 | 本机安装 NI-VISA 后由 IVI USBTMC 正常枚举 |
| T02 | DP831 LAN 菜单灰色 | 检查型号/选件，但继续走 USB DEVICE | LAN 选件与 USBTMC 是两条独立路径 |
| T03 | PyUSB/libusb 看不到 DP831 | 检查 NI-VISA 资源和 NI 后端 | NI-VISA 已工作时，不需要为此替换成 libusb 驱动 |
| T04 | DP831 `VI_ERROR_IO` / `VI_ERROR_TMO` | 关闭其他会话 → 换可靠数据线 → 电脑直连 USB 口 → 单进程重试 → 保存原始错误 | 历史上换端口/直连/单会话后改善，但根因未被完全证明，不能写成唯一结论 |
| T05 | Keithley 无响应 | 直通线 → RS-232 接口 → 9600/8N1 → XON/XOFF → LF → 当前 COM → 资源占用 | 不得用 Null Modem；COM 会变化 |
| T06 | Keithley 有历史错误 | 在取得授权后读取并保存错误队列 | 历史曾见 `-104,"Data type error"`；不能默认由当前脚本产生 |
| T07 | Keithley 并发测量不一致 | 检查 sense function 和 concurrent-sensing 设置 | 历史修正为 `:SENS:FUNC:CONC OFF`，属于状态写入，不能在纯环境验收中自动执行 |
| T08 | 示波器 MCP 重启后失联 | 从示波器重新读取 IP → 更新资源 → 重启/重载 MCP → 再验收 | 历史 IP 改变过；不要硬编码 |
| T09 | Python 能导入但看不到设备 | 分辨“包安装”“后端发现”“资源发现”“身份查询”四个层次 | 导入成功不等于硬件连通 |
| T10 | 多个程序都能看到资源但查询失败 | 确认是否被 NI MAX、UltraSigma、旧脚本或另一个 MCP 会话占用 | 同一资源先保持单会话 |

与自动测试逻辑有关、但不属于驱动安装的问题：固定等待 0.5 秒曾造成 DP831 稳定性误判，后来采用自适应稳定判定；通道时间差 39.207 ms 不是唯一根因；扫描前初始 VI 需要紧邻 Keithley 输出关闭状态回读。这些内容应在测试流程复盘中保留，不能混入“安装成功”验收。

## 9. 部署记录模板

AI 应生成一份新的部署记录，至少包含：

```text
部署日期/操作者：
电脑型号与 Windows 版本：
Python 路径与版本：
虚拟环境路径：
NI-VISA 版本与来源 URL：
CH340 驱动版本与来源 URL：
Python 包冻结清单：

MSO5354：
  连接方式：直连 / 局域网
  电脑 IPv4：
  仪器实际 IPv4：
  VISA 资源：
  L0/L1/L2/L3 验收结果：

Keithley 2400：
  电缆：直通 DB-9（确认：是/否）
  转换器硬件 ID：
  COM：
  串口参数：9600, 8N1, XON/XOFF, LF
  VISA 资源：
  L0/L1/L2/L3 验收结果：

DP831：
  端口：后面板 USB DEVICE（确认：是/否）
  硬件 ID：
  驱动提供商/服务/INF：
  VISA 资源：
  L0/L1/L2/L3 验收结果：

Codex MCP：
  配置层级：用户 / 项目
  server executable：
  resource 参数：
  codex mcp list：

错误原文、发生时间、复现步骤、处置、是否真正解决：
未执行项目及原因：
```

不要在部署记录中保存账号、密码、Cookie、Token 或其他密钥。

## 10. 可直接交给另一个 AI 的执行提示词

```text
你要在 Windows 电脑上协助搭建芯片自动化测试的仪器通信环境。先完整阅读本手册，再行动。

目标链路：
1. RIGOL MSO5354：Ethernet → TCP/IP VISA → PyVISA-py/pymso5000 → Codex STDIO MCP。
2. Keithley 2400：直通型 DB-9 RS-232 → CH340/CH341 USB 转串口 → COM → PyVISA-py。
3. RIGOL DP831：后面板 USB DEVICE → USBTMC → NI-VISA → PyVISA。

执行规则：
- 先做只读基线盘点，列出计划、官方 URL、将改变的内容和回滚办法，等我确认后再安装。
- 只从 Python、NI、RIGOL、Tektronix/Keithley、WCH、PyPI、GitHub 项目和 OpenAI/Codex 官方页面下载。
- 不使用第三方驱动站，不替换现有 USB 驱动，不用 Zadig，除非先说明必要性并得到我的明确许可。
- 不记录任何账号、密码、Cookie、Token 或私密配置。
- 每安装一个组件就记录版本、路径、来源 URL 和验证结果；不要一次性安装完才汇报。
- IP、COM 和 VISA 资源必须现场发现，不能照抄示例。
- 先分别验证三台仪器；不要运行芯片测试。
- 未经我明确授权，不连接或控制任何仪器，不发送 SCPI，不执行 *IDN?，不读取/清空错误队列，不修改设置，不切换任何 OUTPUT。
- 当我授权通信后，仍按 L0 安装 → L1 系统枚举 → L2 资源发现 → L3 *IDN? → L4 白名单只读 → L5 状态写入逐级验收。
- SYST:ERR? 会消耗错误队列，不能当成无副作用读取。
- 同一台仪器保持单会话，遇到错误保存原文，不反复盲试。
- 最终交付部署记录、版本清单、资源映射、MCP 配置片段、每级验收结果、问题台账和未执行项。
```

## 11. 本机已验证基线（仅用于复现）

- Python 3.14.4 x64
- PyVISA 1.16.2
- PyVISA-py 0.8.1
- pyserial 3.5
- PyMeasure 0.16.0
- pymso5000 0.2.0
- NI-VISA 26.5.0 / 包版本 26.50.49318（本地历史安装，不代表当前官网最新版）
- Scope 环境：`D:\ai_scope`
- 自动化工程环境：独立项目虚拟环境（具体路径已脱敏）

历史身份回读：

- `RIGOL TECHNOLOGIES,DP831,<device-serial>,<firmware-version>`
- `KEITHLEY INSTRUMENTS INC.,MODEL 2400,4631606,C12 Mar 31 1998 18:20:20/A02 /F/F`
- 示波器型号：RIGOL MSO5354

这些值用于证明本地链路曾工作，不是新环境的预期固定返回。
