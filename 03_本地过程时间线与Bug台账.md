# 本地过程时间线与 Bug 台账

## 1. 记录范围

本时间线来自 2026-08-31 至 2026-09-02 的本地 Codex 会话、自动化测试工程、结构化日志、PowerShell 命令历史和用户确认的四份网页版 ChatGPT 对话整理稿。它描述的是多次工作会话中的环境搭建和问题收敛，不是逐秒操作录像。

本地原始会话体量很大：三份 JSONL 合计约 39.1 MB，按历史估算约 958 万 tokens；连同命令历史和脚本，粗略总量约 963 万 tokens。因此本记录采用“阶段总结 + 结构化日志 + 关键会话片段 + 最终文件状态”的选择性证据策略，没有把全部原始对话逐字重排。

## 2. 时间线

### 2026-08-31：MSO5354、pymso5000 与 MCP

- 盘点并使用已有的示波器专用 Python 环境；公开版不保留本机绝对路径。
- 本机安装的 `pymso5000` 版本为 0.2.0，并带 MCP 入口。
- 示波器通过 TCP/IP VISA 资源接入；历史地址在网络状态变化后发生过变化，公开版不保留具体 IP。
- 由此确认：MCP 配置与示波器实际 IP 强关联，重启或更换网络后必须重新发现和更新，不能依赖旧地址。

### 2026-09-01 至 2026-09-02：三仪器自动化环境与排查

- 建立/完善自动化测试工程和独立 `.venv`；具体工程名称和路径已脱敏。
- Keithley 2400 通过 CH340 USB 转 RS-232 连接，资源由当前 COM 口生成；历史 COM 号不在公开版保留。
- DP831 初始在 Windows 中以带厂商、产品和设备序列字段的 USB 硬件 ID 出现，但为 Code 28 / `CM_PROB_FAILED_INSTALL`，即接口硬件已枚举而 USBTMC 驱动缺失。
- 纠正/确认物理连接为 DP831 后面板 USB DEVICE；前后面板 USB HOST 不用于这条 PC 控制链路。
- 安装 NI-VISA 26.5.0 后，DP831 由 IVI Foundation USBTMC 驱动正常识别，服务为 `Usbtmc`，NI-VISA 后端为 `C:\Windows\System32\visa32.dll`。
- DP831 最终以 USBTMC VISA 资源被发现，身份回读确认 DP831；公开版不保留具体设备序列号。
- Keithley 串口链路按 9600、8N1、XON/XOFF、LF 工作；身份回读确认 Model 2400。
- 多轮前缀诊断和恢复日志记录了 DP831 I/O/超时、Keithley 错误队列、通道时间差和稳定等待问题。
- 自动扫描最终生成 `rflat_scan.csv` 和 `rflat_curve.png`。这证明该次本地流程产出了结果文件，但本次整理没有重新运行或复核仪器测试。

## 3. Bug 台账

| ID | 现象 | 原因/判断 | 处理或结论 | 证据强度 |
|---|---|---|---|---|
| B01 | DP831 USB 接上但 PC 控制链路不对 | 将 USB HOST 与 USB DEVICE 的用途混淆 | PC 的 USB-A 应接 DP831 后面板 USB DEVICE（USB-B） | 官方手册 + 本地记录确认 |
| B02 | DP831 设备管理器 Code 28 | 缺少适用的 USBTMC 驱动 | 安装 NI-VISA，重启后由 IVI USBTMC 驱动正常枚举 | 安装前后日志确认 |
| B03 | DP831 的 LAN 选项灰色 | 误把 LAN 选件状态与 USB 通信关联 | LAN 与后面板 USB DEVICE 是独立路径；继续走 USBTMC | 手册 + 本地排查确认 |
| B04 | PyUSB/libusb 无法使用 DP831 | 当前可用路径不是 libusb，而是 NI-VISA/USBTMC | NI-VISA 正常时无需换成 WinUSB/libusb，不用 Zadig | 本地最终状态确认 |
| B05 | DP831 偶发 `VI_ERROR_IO` / `VI_ERROR_TMO` | 与线材、USB 端口、会话占用或时序相关的可能性存在 | 直连电脑、换端口/线、保持单会话后改善 | 改善已记录；**唯一根因未证明** |
| B06 | 把 `SYST:ERR?` 当纯只读 | 查询会弹出并消耗错误队列的一项 | 将其列为有副作用查询；使用前告知并完整记录 | SCPI 语义确认 |
| B07 | Keithley 返回 `-104,"Data type error"` | 错误可能在当前诊断前已存在 | 记录为预存错误，不能直接归因于当前命令 | 本地错误队列记录；来源未完全证明 |
| B08 | Keithley 并发测量结果不一致 | concurrent sensing 设置与预期不一致 | 历史流程设置 `:SENS:FUNC:CONC OFF` | 本地流程确认；属于状态写入 |
| B09 | DP831 固定等待 0.5 秒导致稳定性误判 | 固定时延不能覆盖实际稳定时间 | 改为自适应稳定检查 | 代码/诊断记录确认 |
| B10 | CH1/CH2 采集有约 39.207 ms 间隔 | 曾怀疑时间差导致异常 | 证据表明它不是唯一根因 | 日志确认间隔；因果被否定 |
| B11 | 扫描起点的初始 VI 状态难以解释 | 状态读取与输出关闭时刻不够邻近 | 初始 VI 需紧邻 Keithley OFF 状态回读和记录 | 流程修正记录确认 |
| B12 | 开发/诊断工具出现临时错误 | 测试脚本、命令或工具调用在迭代中有失误 | 保留为开发过程问题，不当作仪器故障 | 会话和日志可见 |
| B13 | 终端显示输出被截断 | 界面/输出长度限制 | 以完整 CSV 文件作为结果核对来源 | 结果文件确认 |
| B14 | README 与最终脚本能力不完全一致 | 文档更新滞后于代码迭代 | 后续应以最终 CLI、日志和本整理包交叉核对 | 文件状态确认 |

## 4. 关键结论和可复用经验

### 4.1 “连接成功”必须分层

```text
驱动安装成功
  ↓
Windows 正确枚举设备
  ↓
VISA/COM 资源可发现
  ↓
*IDN? 身份匹配
  ↓
白名单只读查询符合预期
  ↓
经授权后才允许状态写入和输出动作
```

任何一层通过都不能替代下一层。尤其是 Python 包可导入、MCP 工具出现或 VISA 资源可列出，都不等同于仪器身份已确认，更不等同于自动测试可安全运行。

### 4.2 资源标识要动态发现

- 示波器 IP 会随网络状态变化。
- CH340 的 COM 号会随 USB 端口和系统枚举变化。
- DP831 VISA 资源中的序列号属于具体设备。

因此配置应该保存“发现方法”和“上次已知值”，而不是只保存硬编码地址。

### 4.3 三条链路应独立验收

先分别确认 MSO5354、Keithley 2400 和 DP831，再进入跨仪器同步和自动扫描。这样故障可以定位在网络、串口、USBTMC、驱动后端或测试逻辑中的具体一层。

### 4.4 资料中的事实等级

- **已确认事实**：有官方手册、本地结构化日志或最终文件支持。
- **历史工作值**：曾在该电脑上有效，但可能漂移，例如 IP、COM、资源序列号。
- **推断/待证**：现象改善但没有唯一根因证明，例如 DP831 偶发 I/O/超时。
- **未复验**：本次只读整理未重新连接仪器或运行测试。

## 5. 本地阶段未做的事

- 未连接或控制 MSO5354、Keithley 2400、DP831。
- 未发送 `*IDN?`、`SYST:ERR?` 或任何其他 SCPI。
- 未切换 OUTPUT、未修改仪器状态、未运行芯片测试。
- 未重新验证 `rflat_scan.csv` 的电气真实性或复算曲线。
- 已读取四份网页版 ChatGPT 对话整理稿；未获得逐条消息、时间戳、原始图片与附件映射形式的完整会话导出。
- 未撰写跨本地工程、日志和网页版整理稿的最终完整项目报告。
